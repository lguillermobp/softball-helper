[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && node prisma/seed-safe.js && npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
